import { ArticleInterface } from 'src/app/auth/shared/types/article.interface';

// just for article save/updates requests grouping from form
export interface SaveArticleResponseInterface {
  article: ArticleInterface;
}
